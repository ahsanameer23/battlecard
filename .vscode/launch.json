{
    "configurations": [
        {
            "name": "Containers: Node.js Launch",
            "type": "docker",
            "request": "launch",
            "preLaunchTask": "docker-run: debug",
            "platform": "node",
            "node": {
                "package": "${workspaceFolder}/omada-nexus/package.json",
                "localRoot": "${workspaceFolder}/omada-nexus"
            }
        }
    ]
}